<form {{action 'handleFilterEntry' on='submit'}} class="form-inline">
<div class="input-group">
  {{input value=value
          class="light"
          placeholder="Filter By MeSH"}}
  <div class="input-group-btn">
    <button type="submit" class="btn btn-success">Search</button>
  </div>
</div>
</form>
{{yield results}}
